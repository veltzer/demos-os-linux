# Web server setup exercise

I want to set up a web server.

1. Find and install a service which is a web server.
2. Make it listen on port 8081 (change its configuration).
3. Point your browser to:
   `http://localhost:8081`
   and see that the service is working.
